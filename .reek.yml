---
detectors:
  TooManyStatements:
      max_statements: 8
  IrresponsibleModule: # Should be able to remove later!!!
    enabled: false
  DuplicateMethodCall:
    allow_calls:
      - match
      - config