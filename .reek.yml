---
detectors:
  TooManyStatements:
      max_statements: 13
  IrresponsibleModule: # Should be able to remove later!!!
    enabled: false
  FeatureEnvy:
    enabled: false
  DuplicateMethodCall:
    allow_calls:
      - match
      - config
      - bar